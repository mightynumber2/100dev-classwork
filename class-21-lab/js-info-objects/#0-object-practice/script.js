///////////////////////////////////////
// OBJECTS

/* const users = new Object();

users.name = 'Dan';
users.age = '41';
users.gender = 'male';

delete users.gender;
users['likes programming'] = true;
delete users['likes programming'];

let key = 'likes programming';
users[key] = true;
delete users[key];

key = prompt("What do you want to know about the user?", 'name, age, or gender');
key = 'name';

alert(users[key]);
console.log(users);

let fruit = prompt('Give me a fruit to make a tech company with:');

const bag = {
  [fruit + 'Computers']: 5,
};
console.log(bag); */

/* function makeUser(name, age) {
  return {
    name,
    age,
    gender: 'male',
  };
}
const newUser = makeUser('Dan', 41);
console.log(newUser); */

/* const users = {
  age: 41,
  job: 'software dev',
};

// alert(users.name === undefined);
// alert('name' in users);
// alert('age' in users);
let key = 'age';
alert(key in users); */

/* const users = {
  name: undefined,
};

alert(users.name);
alert('name' in users); */

/* let user = {
  name: "John",
  age: 30,
  isAdmin: true
};

for (const key in user) {
  console.log(key);
  console.log(user[key]);
} */

/* let codes = {
  "+49": "Germany",
  "+41": "Switzerland",
  "+44": "Great Britain",
  // ..,
  "+1": "USA"
};

for (const code in codes) {
  console.log(+code);
} */
