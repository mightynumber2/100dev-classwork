//Write your pseduo code first! 

// You would need the temp in celsius to convert
const tempCelsius = 50;

// then you would need to convert to farenheit 
function calcToFaren(temp) {
  return (temp * 1.8) + 32;
}

// then show the calculation
console.log(calcToFaren(tempCelsius));
